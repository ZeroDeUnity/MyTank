using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ParameterMananger : MonoBehaviour
{
    public Animator Player_1_Animator;

    public Player PlayerScript;


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //Player_1_Animator.SetBool("IsIdle", false);
        //Player_1_Animator.SetFloat("VerticalSpeed", 0);
        //Player_1_Animator.SetFloat("HonzontalSpeed", 1);
    }
}
